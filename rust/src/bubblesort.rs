pub fn sort(vec: Vec<u8>) -> Vec<u8> {
    println!("Bubble sort!");
    // let mut vec = vec;
    // let mut n = vec.len();
    // let mut swapped = true;
    // while swapped {
    //     swapped = false;
    //     for i in 1..n {
    //         if vec[i - 1] > vec[i] {
    //             vec.swap(i - 1, i);
    //             swapped = true;
    //         }
    //     }
    //     n -= 1;
    // }
    vec
}
